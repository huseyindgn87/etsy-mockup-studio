<!-- BEGIN:nextjs-agent-rules -->

# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` (resolved from this file's directory; in monorepos the `next` package may not be visible from the repo root) before writing any code. Heed deprecation notices.

This block is written and re-added by `next dev` — verify at `node_modules/next/dist/server/lib/generate-agent-files.js`. Removing it from a diff only re-creates the uncommitted change; committing it with your work keeps the tree clean.

<!-- END:nextjs-agent-rules -->

# Keeping this file current (mandatory)

At the end of every session, update the **Project state** section below (and
CLAUDE.md if anything it says changes) so it matches reality, and commit it.
Verify every fact before writing it — HEAD from `git log`, the test count from
an actual `vitest run`, pending manual steps from the migrations folder and
`.env.example`. Never carry forward a number or status you didn't re-check.

# Project state

_Last updated 2026-09-18._

- **HEAD:** feaa104 plus the brute-force commit (which updates this file). `tsc --noEmit`, `eslint .`, `next build` clean; **1298 vitest tests passing**.
- **`npm test` runs `next build && vitest run`**, so a file the bundler can't parse fails the test command (vitest alone passed while `BulkEditor.tsx` didn't parse in dev — that error, logged by `next dev`, came from a mid-edit state; the committed file builds). `npm run test:unit` is vitest only.
- **Shipped:**
  - **Brute-force protection** (`lib/auth/throttle.ts`, counters in the `auth_throttles` table — migration `20260918150000_auth_throttles`, applied 2026-09-18). Per account (keyed by the email's SHA-256, so an unknown email locks and escalates exactly like a real one): every 3 failures — wrong password or wrong 2FA code, one shared count — lock it for 3 min, then 15 min, then 1 h; reaching the 1-hour level also sets `emailLocked`, which blocks even the right password with a message until an email unlock exists (so "1 hour for each further 3 failures" never comes into play). A successful sign-in deletes the row. After the first lock every attempt (password or 2FA step) needs a Cloudflare Turnstile token verified server-side (`lib/auth/turnstile.ts`; test keys outside production, `TURNSTILE_SITE_KEY`/`TURNSTILE_SECRET_KEY` otherwise); a missing/failed check isn't counted as a failure. Per IP: 20 failed sign-ins per 15 min (`login-ip:`), 5 sign-ups per hour (`register-ip:`, 429 with minutes to wait). IP from `cf-connecting-ip` / `x-real-ip` / first `x-forwarded-for`, else one shared "unknown" bucket. Codes reach `/login` as `CredentialsSignin` codes (`account_locked:<ms>`, `rate_limited:<ms>`, `email_verification_locked`, `human_check_required`, `human_check_failed` in `lib/auth/two-factor-codes.ts`); the page says when to retry, a lock on the 2FA step sends the user back to the password step, and the widget (`app/components/Turnstile.tsx`) remounts after each use. `/login/page.tsx` is now a server wrapper passing the site key to `LoginForm.tsx`. **No email-existence leaks:** registration answers the same 201 `{ ok: true }` whether or not the email is taken (the existing account is untouched; `/login?registered=1` now reads "Done — sign in below…"), a sign-in with an unknown email is compared against a dummy bcrypt hash so it takes as long as a wrong password, and `/settings`' email change says "That email address can't be used" instead of "already exists". Unit/jsdom tests only — the Turnstile widget has not been looked at in a real browser.
  - **One definition of "a change", everywhere** (`lib/etsy/listing-changes.ts`): two states are the same only when every value is deep-equal (sorted-key JSON, the same key the draft autosave uses); nothing is exempt. A difference Etsy's API can't write is an `UnsyncedChange` (field + reason) shown to the user, never dropped.
    - **Sync to Etsy in the single editor** (existing listings only, header button beside Save draft / Preview / View on Etsy) re-reads the listing's form from `GET /api/etsy/listings/[id]/editor` and diffs **every** form key (`lib/etsy/editor-sync.ts` — a `Record<keyof ListingFormValue, group>` so a new form field doesn't compile until it has a writer), and diffs the photo/video grid (order, alt text, added/removed photos and videos) against the listing's Etsy media. Grid changes go first through the render route's existing-listing save (`POST /api/mockups/render`, `editExisting`), after which the grid is reset to what Etsy then holds (rendered/own tiles leave the grid, so a second Sync doesn't re-upload); field changes go through bulk edit's per-listing write (`syncListingPatch`). Previously a photo reorder reported "Nothing to sync". Now also written: **Feature this listing** (`featured_rank: 1`, new `featuredRank` on `BulkListingPatch`) and each **variation property's scale** (`scale_id` on inventory property values — form, bulk parse, `updateListingInventory`, publish; the editor loader now keeps Etsy's scale instead of dropping it on every grid write). Reported as not synced, with the reason: un-featuring (the spec has no un-feature value), Etsy Ads (no endpoint), removing the section or category, a blank/invalid price or quantity, clearing the processing profile or an attribute, variation settings on a listing without variations, and a variation photo that isn't on Etsy yet. With anything unsynced the unsaved flag stays set. jsdom/unit tests only; never run against a real shop.
    - **Unsaved changes in the editor are a state difference, not an edit count**: `committed` holds the draft snapshot as loaded / explicitly saved / written to Etsy; after a user edit, any difference from it is unsaved (typing then undoing is not). State that settles on its own before the next edit (seeding, reconciling, uploads finishing) is absorbed into `committed`. Pending uploads still count unless the revision was just written to Etsy.
    - **Bulk edit** uses the same equality for rows and media (`sameState`, `mediaStateFromGrid`/`mediaStateFromEtsy`). Edits it used to drop silently — a blank title, an emptied tag/material list, a cleared attribute/section/category/weight/size, price/quantity/SKU/processing typed on a variation listing's row, removing a grid's last variation — are now counted as pending (Sync count, unsaved warning), reported per row on Sync ("Not synced to Etsy: Title (Etsy requires a title)", "Partly saved." when the rest landed) and kept on the row; **Schedule refuses** a job while any such edit is pending, naming it.
  - **Toasts replace the editor's and bulk edit's result banners** (`app/components/toast/`: `ToastProvider` mounted in `app/(app)/layout.tsx`, `useToast().show({ message, kind, id })`; without a provider `show` is a no-op, so component tests wrap in `<ToastProvider>`). Bottom-left stack of speech-bubble cards with a × button; success/info are `role="status"`, errors `role="alert"`; the same `id` replaces a toast instead of stacking. Each fades out after 6 s (`TOAST_DURATION_MS`), paused while hovered or focused. Enter slides up and fades in, then shakes and pops 3–5 "$" through the shop button's `burstDollars` (new optional `{ count }`); `TOAST_ANIMATION` in `config.ts` switches all motion off, and under `prefers-reduced-motion` it only fades. Moved into toasts: the editor's error banner, Synced/Nothing to sync, the Publish/Save to Etsy result, the schedule confirmation and a failed schedule, the listing-media load error; bulk edit's load error, missing listings, save error, the per-run "Updated N of M" summary (per-row results stay on the rows) and the schedule confirmation. The two grey header notices are gone: "…can't be scheduled" is a toast when Schedule is pressed (the button is no longer disabled for that reason), and "photos and videos become exactly what the grid shows…" is a toast on the first photo/video grid edit of an existing listing. Verified in jsdom only — not yet looked at in a real browser.
  - The listings toolbar's selection action is labelled **Bulk edit** (was "Edit").
  - **Lists reach Etsy comma-joined, and a saved row shows what Etsy confirmed** (`lib/etsy/form-list.ts`, `lib/etsy/listing-confirmed.ts`). Etsy documents `tags`/`materials` as "a comma-separated list" in one urlencoded field; the app sent repeated params (`tags=a&tags=b&tags=c`) and Etsy's parser keeps only the **last** one, so a bulk Sync left the listing with a single tag and silently dropped its original ones — observed against a real shop. `joinList`/`joinIdList` now build one field for `tags`, `materials`, `production_partner_ids` (both `createDraftListing` and bulk `updateListing`) and `value_ids`/`values` (`setListingProperty`); a comma inside a value is replaced with a space so one value can't split into two. The single editor's **Publish had the same defect** — every listing published before this holds only its last tag, worth checking on Etsy. JSON-bodied writes (inventory, variation images, personalization) were never affected. `updateListing`'s response is now parsed (`confirmedListingFields`) and returned on `BulkResult.confirmed`; the bulk row (`applyConfirmed`) and the cached listing row are refreshed from it instead of from the patch that was sent, so the screen can't show a list Etsy didn't store. Only fields Etsy echoes are taken — price/quantity/SKU/personalization still come from their own writes, and a response that isn't a listing falls back to the patch.
  - Listing editor at `/mockups` (variations, photos + video, personalization, settings tab, price-by-variation).
  - `/listings` backed by a DB listings cache with on-demand refresh; multiple Etsy shop connections per user (refresh tokens encrypted at rest).
  - **Full listing sync** (`lib/etsy/listing-sync.ts`, schema from migration `20260918090000_extend_listing_cache`): Refresh now stores every listing field the schema holds (description, tags, materials, type, who/when made, supply, price as amount/divisor/currency, shipping profile, return policy, weight/dimensions, processing min/max, personalization), images (alt text, rank), videos (slot rank) and the whole inventory grid (`listing_inventory_properties/values/products/product_values`). Three stages reported to the refresh modal (step N of 3 + progress bar): pages with `includes=Images,Videos,Personalization`; batched upserts (100 listings per transaction, images/videos replaced); one `GET /listings/{id}/inventory` per listing at 4 req/s (`createRateLimiter`), committed 25 at a time together with that listing's `syncedAt`. Resumable: a listing whose `syncedAt` is newer than the connection's `lastSyncedAt` (set by the route only after a full success), less than 24 h old and not modified on Etsy since, is skipped on a re-run. A 404 inventory stores the listing without a grid; any other failure stops the run with the finished batches kept. Read-only against Etsy (GETs only; a test asserts it). `listing.sku` now comes from the page's `skus` field instead of the batch inventory call. `removedAt` behaviour unchanged. Nothing reads the new tables yet.
  - Listing drafts (DB rows + R2 binaries).
  - App accounts (Auth.js credentials, "Keep me signed in"), scoping all user data.
  - Mockup template library + calibration screen at `/admin/templates` (last commit touching it was `5392d17 wip` — confirm with the maintainer what, if anything, is left). **Admin only** (`lib/auth/admin.ts`): the page answers `notFound()` and `PUT /api/admin/templates/[filename]` a 404 unless the signed-in user's id is in `ADMIN_USER_IDS` (ids, not emails — emails aren't verified, so an unclaimed admin address could be registered by anyone). Unset = nobody, including the maintainer. Before this any signed-in account could edit the shared library.
  - **Raw template images never reach a regular user.** **All template files live in R2** (`lib/mockup/template-store.ts` reads no local disk; a test scans app/ and lib/ for `public/templates` or a local templates folder): library files at `templates/library/{filename}`, user uploads at `templates/user/…`, metadata in `MockupTemplate` rows. The library is the set of `source: "library"` rows (no longer a folder listing); a row is created only by `npm run templates:import [dirs…]` (`scripts/import-templates-to-r2.ts`, default `templates/` + `public/templates/`), which uploads each JPEG/PNG, creates an uncalibrated row if missing, re-reads it from R2 byte for byte, and is safe to re-run (same-size objects aren't re-uploaded, existing rows keep their calibration) — it is also how new library templates are added. Run against the real bucket 2026-09-18: the one library file (`COMFORT COLORS 1717 - Grey - Mockup.jpg`, calibrated row kept) is in R2; a second run changed nothing. `DELETE /api/mockups/templates/[id]` deletes a user's own upload (R2 object first, then the row). Every template image — library (`GET /api/mockups/templates/library/[filename]/image`) or a user's own upload (`.../[id]/image`) — is answered as a ≤1400 px JPEG with the LISTHOUSE wordmark tiled across it (`lib/mockup/template-preview.ts`), with the original size in `X-Template-Width/Height`; the raw file (`?raw=1`) only for an `ADMIN_USER_IDS` user (the admin calibrator uses it), a 404 for anyone else. The editor keeps only the preview for a template mockup (`MockupItem.template`) and renders name it by `TemplateRef` in the render payload (`mockups[i].template`, no `mockup` file part); the render route reads the raw bytes server-side (`readTemplateForRender` — a library file only if it's in `templates/`, a user upload only for its owner). Scheduling renders a template combo on the server too (`single: true` answers one image instead of a ZIP); PSD mockups still render in the browser. Library templates have no delete route and editing them is admin-only. The repo's `templates/` folder is deleted (its file is in R2); `npm run templates:import` with no arguments now finds nothing to import unless the folder is recreated.
  - **Placeholder wordmark in the mockup preview** (`MockupCanvas`): with no design placed, a faint LISTHOUSE sits centred in the preview frame as a DOM layer over the canvas; it goes when a design is added and returns when it's removed. It is never drawn into the canvas or any render. jsdom only — not yet looked at in a real browser, nor the tiled preview watermark.
  - Account settings at `/settings`: Light/Dark theme stored per user and rendered server-side as `<html data-theme>`, first/last name, email change (requires current password), password change, Etsy connection status + Disconnect (the only place these live), Log out.
  - Top bar reduced to a sidebar toggle (listings page), the **LISTHOUSE** wordmark (links to `/listings` — the app's home, showing the currently selected shop — from every page including the `/` shop picker; inert text only on `/listings` itself), and an avatar menu (Account settings, Sign out / Sign in).
  - **The product is named Listhouse** (`lib/brand.ts` → `APP_NAME`): login/register cards, page titles (`%s · Listhouse`), the 2FA authenticator issuer, and the uppercased wordmark all read it. "Etsy Mockup Studio" is gone from the UI (`app/__tests__/branding.test.ts` guards this); the repo/package name `etsy-mockup-studio` is unchanged.
  - All glass cards (home, login, register) are still — the hover sheen and its `entry-card` styles are deleted. Clicking the shop button plays a ~1s "$" particle burst (`app/(app)/dollar-burst.ts`, Web Animations API, skipped under reduced motion) — deliberately tight, staying within ~250px of the click point.
  - Password inputs everywhere use `app/components/PasswordInput.tsx` (show/hide toggle).
  - Optional TOTP two-factor auth: enable/disable on `/settings` (QR via `qrcode`, TOTP on `node:crypto` in `lib/auth/totp.ts`), second sign-in step on `/login` accepting a 6-digit code or a single-use recovery code (10 issued, SHA-256 hashed). Secret AES-256-GCM encrypted with `TWO_FACTOR_ENCRYPTION_KEY`; codes can't be replayed.
  - The editor's **"Schedule for later" picker is a popover anchored under its button** (`ScheduleDialog`'s new `anchored` prop, used only there): the button lives in the sticky header, whose `backdrop-filter` makes it the containing block for `position: fixed` children, so the centred modal came out over the header and its first lines were clipped. Anchored it renders inside a `relative` wrapper beside the button at `top-full right-0 mt-2 z-50`, never flips upward, and scrolls inside itself (`max-h-[min(70vh,34rem)] overflow-y-auto`) when there's no room below; a press outside closes it. `/schedule`'s Reschedule and bulk edit's Schedule keep the centred modal.
  - **Listing scheduling (parts 1 and 2 — data model, API, UI, runner):** `ScheduledListing` (draft, shop, owner, UTC `scheduledAt`, IANA `timezone`, status pending/publishing/published/failed/cancelled, `publishSpec` + `renderSetId` + `images` fixed at schedule time, `attemptCount`/`nextAttemptAt`/`lastError`/`etsyListingId`; index on status + scheduledAt, unique `activeDraftId`). `lib/scheduling/` holds Intl-only wall-time⇄UTC conversion (DST-gap times rejected), validation, a store scoped to user **and** active shop, the runner and the publisher. API: `GET/POST /api/schedule` (`from`/`to` range or `draftId`), `PATCH /api/schedule/[id]`, `POST /api/schedule/[id]/cancel`, `PUT|DELETE /api/schedule/renders/...`, `POST /api/schedule/run`. `/schedule` (listings sidebar) shows a two-week strip with TODAY, fortnight arrows, reschedule/cancel per entry.
  - **Rendering happens at schedule time, in the browser** (`app/(app)/mockups/schedule-renders.ts`): full-resolution `compose()` + the same JPEG window as Publish, uploaded to `scheduled/{userId}/{renderSetId}/image-NN` in R2. The runner never composites — it creates the listing, attaches the stored images in rank order (`overwrite`, so retries are idempotent), activates it, then deletes **only** that job's own keys (`isOwnedRenderKey`); user uploads under `drafts/` and `templates/user/` are never touched. Cancelling a schedule — or deleting a draft that has one — deletes its rendered images the same guarded way (nothing will publish them any more; scheduling again re-renders). Failures retry with backoff (5 min, 20 min) up to 3 attempts, then "failed" with the images kept. Claiming is a conditional pending→publishing update, so concurrent runs can't double-publish; `etsyListingId` is recorded as soon as Etsy creates the listing so a retry finishes it instead of duplicating. Runner auth: `Authorization: Bearer $SCHEDULE_RUNNER_SECRET`; run it with `npm run schedule:run` (needs `npm run dev` up). Listing-creation steps are shared with the editor's Publish in `lib/etsy/publish-listing.ts`; the runner authenticates from the shop connection's stored refresh token (no cookie).
  - **Photo/video tiles — one shared component** (`app/components/listing-media/`: `ListingMedia.tsx` with `ListingMediaEditor`, pure order/alt-text helpers in `photo-order.ts`, the existing-listing grid model in `existing-media.ts`), used by the create editor, the existing-listing editor (`/mockups?mode=existing`), copies, and every row of `/listings/bulk` Media: photo and video tiles drag to any position, with Move left/right buttons that appear when a tile has keyboard focus; an X on each filled tile removes it in one click (a rendered mockup×design combo is remembered in `removedJobKeys`, saved with the draft, so it stays out of the grid); hovering a photo shows an "Alt text" button that opens the enlarged view with the field focused and a "N characters remaining" counter (500 max), and tiles that have alt text keep a filled "✓ Alt text" badge; empty slots (all 20 photo slots, and the video slots) show a plus and open the file picker. Photo tiles are ~15% larger (grid 4 / sm 5 / md 7 columns). Everything stays in the editor — the photo order and alt text go to Etsy only on Publish/Schedule/Sync updates. **Videos are saved with drafts** (`lib/drafts/videos.ts`): `photosData.videos` keeps one entry per slot in order — a picked file as an R2 asset (`drafts/{id}/video/{itemId}`, new asset kind `video`, 100 MB cap) or an Etsy video by id — and a reopened draft restores them in the same slots. A picked file is saved as an empty slot until its upload finishes. Drafts saved before this have `videos: null` and restore as before.
    - **Existing listing** (`mode=existing`): the grid starts from the listing's current Etsy photos (alt text filled in) and videos, read via `GET /api/etsy/listings/bulk?ids=`, plus any renders/own photos added. **Save to Etsy** (was "Add"; the "replace existing images" checkbox is gone) makes the listing's photos and videos exactly what the grid shows, through the render route with `editExisting` + `videoOrder`. Publishing is blocked until the listing's photos have loaded, so a failed read can never wipe them. Photos removed from the grid are saved with the draft (`removedEtsyImageIds`, no migration — it's inside `photosData`).
    - **How a saved grid reaches Etsy** (`lib/etsy/listing-media-edit.ts`, used by the editor's Save/Sync to Etsy, bulk Media on Sync updates, and the scheduled bulk runner): **photos are never deleted to reorder them.** `uploadListingImage` with an *attached* photo's `listing_image_id` + `rank` re-ranks it in place — same id, same file, alt text kept. Etsy documents that parameter only for re-attaching deleted photos, so it was verified against a real listing on 2026-09-18 (4457979056, 18 photos; photo 3 moved to rank 1 became the thumbnail, then the original order was restored and re-checked after 30 s). One such call **sets** that photo's rank without moving any other, so photos can tie and the reads after it looked inconsistent; writing rank 1…n for every photo, in order, gave exactly that order every time. A save: uploads genuinely new photos first, as the 20-photo cap allows; deletes only photos the user removed, never the listing's last one (Etsy refuses: "Listings must have at least 1 image"), uploading the rest of the new ones as room frees; writes every photo's rank (with its alt text — an empty `alt_text` is sent only to clear a photo's text); then re-reads `GET /listings/{id}/images` and reports any photo not at its grid position or without its alt text ("Photo order: …"). A photo Etsy won't delete is reported and put last. Ids not on that listing are refused, the current media is re-read from Etsy first, and failures never stop the rest. The old save (keep the unchanged leading run, delete the rest, re-attach) turned "move photo 3 to 1" into deleting every photo; Etsy refused the last delete, which is why the old last photo became the thumbnail. Videos have no rank; they still keep their unchanged leading run and the rest are deleted and re-attached by `video_id` in slot order.
    - **Bulk Media** is editable per row (tiles, alt text, upload, videos) and saved on Sync updates by `POST /api/etsy/listings/[id]/media` (ownership-checked like the other bulk routes, one request per changed listing, refuses an empty photo set).
    - **Copies**: the editor's copy prefill now carries each source photo's alt text, and a draft made by the listings page's bulk Copy fills itself from its source listing the first time it's opened (it previously opened empty).
  - Scheduling is refused, with a message, for "add photos to an existing listing" mode and for listings with a video; Publish is disabled while a draft has a pending/publishing schedule. The video refusal stays even though drafts now keep videos: the runner (`lib/scheduling/publisher.ts`) only uploads the images stored under `scheduled/…` at schedule time — it has no video snapshot or video upload step, and reading the draft's own video keys at run time would publish whatever the draft holds then, not what was scheduled.
  - **Existing-listing editor loads the listing's fields** (`GET /api/etsy/listings/[id]/editor` → `lib/etsy/listing-editor-load.ts`, pure mapping in `lib/etsy/listing-editor-form.ts`): before the form renders ("Loading listing…"), Title, Description, Tags, How it's made, Price, Inventory, Variations (full grid: options, per-combination price/quantity/SKU/processing profile, disabled combinations), Shipping (processing profile) and Settings (section) come from the cached row. The cache doesn't hold category, category attributes, production partners, personalization questions, featured or auto-renew, so those come from one `/listings/batch` read + one `/properties` read (+ the cached taxonomy tree for the path); if that read fails the cached fields still load with a warning. A listing with no cache row (or never fully synced) is read from Etsy entirely, refused unless its `shop_id` is the active shop. A reopened existing-mode draft whose form has no title (the blank drafts the old autosave wrote) is hydrated the same way. Verified against the real cached rows for SKU HG-000179 (listings 4440391068 and 4440405731) via the mapper; the live-supplement half and the page in a real browser session have not been run.
  - **The listing editor (`/mockups`) is one scrolling form, not tabs** (`app/(app)/mockups/editor-sections.tsx`, `useSectionNav.ts`): every section (Photos … Settings) is mounted at once, stacked in sidebar order as a card with an `h3` heading and anchor id (`#photos`, `#title`, … `#how-its-made`, … `#settings`). The sidebar is sticky below the header and holds `<a href="#…">` links: a click (or Enter) smooth-scrolls there — instantly under `prefers-reduced-motion` — moves focus to the section heading, and `replaceState`s the hash (no history entry). The active item (`aria-current="location"`) follows an IntersectionObserver whose root excludes the sticky header; when several sections are in view the topmost in sidebar order wins, and a 50vh spacer below Settings lets the last sections reach the top. A section hash on load (or `hashchange`) scrolls there once loading has settled; otherwise a reopened draft scrolls to its saved section (`photosData.activeTab`, still stored, but excluded from the autosave snapshot so scrolling never writes a draft). A refused Publish (`publishErrors` in `page.tsx`, each reason tagged with its section) scrolls to the first errored section in sidebar order and marks every errored sidebar item with a red "!" until fixed; "Schedule for later" still only reports the message in its dialog. Publish is no longer disabled for a missing processing profile, so that case reaches the error jump. Verified by jsdom tests only (fake IntersectionObserver, stubbed `scrollIntoView`) — not yet looked at in a real browser.
  - **Variations section rebuilt Vela-style, part 1** (`app/(app)/mockups/VariationsSection.tsx`): cascading Category → Sub-category → Group → Item type dropdowns at the top (`lib/etsy/taxonomy-cascade.ts`; the form still stores only the deepest chosen node as `taxonomyId`/`taxonomyPath`, a deeper Etsy branch adds a dropdown, changing a level clears those below and — like the Details picker — clears the variations), then an inner tab bar (Variations, Price, Quantity, SKU, Visibility, Photos, Processing; arrow-key navigation) with a Show less/Show more toggle that collapses everything but the tab bar. The Variations tab has three columns: a property dropdown (the leaf's variation properties, minus ones used in other columns, plus "Create your own…" for Etsy's custom 513/514/516 slots with a name field), a delete icon, a scale select when the property has scales (optional `scaleId` on `ListingFormVariation`, sent as `scale_id`), value rows with a drag handle (drag within the column, or ArrowUp/ArrowDown on the handle) and ×, and an add box that matches Etsy's listed values by name (datalist) or adds free text. All column edits and the shared combination model live in `lib/etsy/variation-combinations.ts`: `createCombinationCache` rebuilds the cross product only when value ids/names/order change (450 rows tested); removing a value or column, replacing a property, changing scale or changing category first computes which combinations would lose price/quantity/SKU/processing-profile cells and asks in an alertdialog ("Removing “S” deletes 2 combinations with price or SKU data."), and a confirmed edit prunes the orphaned cells and visibility flags. Everything persists through the existing `formData` (no migration). The old modal editor, variation cards, vary-by toggles and combination table are deleted (`CustomOptionsInput` stays; Personalization uses it). The taxonomy tree is now fetched once on mount and shared with the Details picker. Removing a value or column also warns when it drops photo assignments; the Details category picker still clears variations without warning. jsdom tests only — not yet looked at in a real browser.
  - **Variations section part 2 — the six per-combination tabs** (`app/(app)/mockups/VariationOfferingTabs.tsx`, pure model in `lib/etsy/variation-offerings.ts`). Price, Quantity, SKU and Processing each show one "Individual <field> (<variation>)" checkbox per variation (= that field's `variationToggles.appliesTo`). With none checked the tab is one listing-wide control — the form's own `price`/`quantity`/`sku`/`readinessStateId`. Checked, the table has **one row per distinct value of the checked variations** (not the full cross product when only some are checked — each row is the one cell those combinations share), leading columns for those values, the control on the right. Checking copies existing values onto the finer rows; unchecking merges rows keeping the first value in grid order and, if any different value would be lost, asks first ("…discards 1 different price."), unchecking the last moves the kept value to the listing-wide field. Every combination table (Price/Quantity/SKU/Processing, Visibility, Photos) is **collapsed to its first 6 rows** with a "Show more (N more)" / "Show less" button below it (`RowTable`; no button at 6 rows or fewer) — the collapse is display only, so hidden rows keep their form state and the bulk bar, filter and SKU generator still reach every row; a jump to a collapsed row (a refused Publish) expands the table first. Beyond that, rows are virtualized (fixed 44 px rows, 440 px viewport, ~20 rows mounted for a 450-row grid) and memoized with stable handlers so typing re-renders only that row. A filter box narrows rows; Price/Quantity/Processing have a bulk bar (Set to / Increase by / Decrease by, plus ± % for price; processing shifts to the profile of the same kind with min and max days both moved, skipping rows with no match) applied to the rows shown, refusing any edit that would make a value negative. Price inputs take digits and two decimals only (shop currency symbol from `/api/etsy/shop`, which now also returns `currencyCode`), padded on blur; quantity digits only. SKU has a pattern generator: `{Size}`/`{1}` value tokens, `{#}`/`{###}` counter from a start number, filling every shown row. Visibility: one row per full combination with an Offered/Hidden switch (`variationRowEnabled`); hidden combinations stay in every tab greyed with "(hidden)" and keep their data. Photos: a single "Photos vary by" selector (sets `linksPhotos` on one variation — Etsy's one-property rule), rows are that variation's values, each with a strip of the photo grid's tiles; the choice is stored per value id in the new form field `variationPhotos` (slot ids, in `formData`, no migration); moving the selector warns before discarding assignments. **Validation** (`validateOfferings`): price required and > 0 with ≤ 2 decimals (a blank row falls back to the listing price), quantity a whole number ≥ 0, SKU ≤ 500, a profile per row when processing varies, and a photo must still be in the first 20 grid slots. Errors mark the row (`aria-invalid`, red row, message) and the sub-tab ("!"), once Publish has been refused; a refused Publish for a "new" listing now includes the first per-combination error, jumps to the Variations section, opens that tab and scrolls to the row. **Payload** (`buildInventoryPayload`, replacing the page's own builder): every combination with price/quantity/SKU/processing profile/`enabled`, the `*_on_property` ids, and `imagesByValue` `{propertyId, valueId, value, imageIndex}` where `imageIndex` is the photo's position in the upload order. The server keeps images for one property only without duplicates, resolves value ids from the inventory Etsy returns (`updateListingInventory` now returns it — free-text/custom values get their ids from Etsy there), then posts `variation-images` with the uploaded image ids — in the render route by upload position, and in the scheduled runner (which now keeps position-based mappings in the stored spec and calls `updateVariationImages` before activating). The old `jobIndex` form still works. jsdom and unit tests only — never looked at in a real browser or sent to Etsy; the value-id lookup by name for custom/free-text values is the first thing to check against a real shop.
  - **Autosave only writes after a user edit** (`page.tsx`): the draft PUT body is a memo compared by sorted-key JSON (`lib/drafts/snapshot.ts`) with the content as loaded (until the first edit) or last saved; nothing is written on mount, during hydration/restore, or when the content is deep-equal. Previously the autosave effect re-armed on every save's status change, so an open draft PUT itself every ~2 s forever. A failed save is no longer retried until the next change.
  - **`/listings` row click opens our editor, and nothing on the row links to Etsy.** Clicking a row (anything but its checkbox cell, the hover action buttons and the title link) goes to `/mockups?mode=existing&listingId=…` — the same destination as the title link and the hover Edit icon, which loads the listing's current photos and videos. The row's "↗ Etsy" pill is gone; the way out to Etsy is now a **View on Etsy** button in the editor header (existing mode only), beside Save draft and Preview, with the orange Etsy badge (`app/components/EtsyMark.tsx`) and `https://www.etsy.com/listing/{id}` in a new tab. The hover Share icon still copies the Etsy link.
  - **Unsaved-changes warning** (`app/components/unsaved-changes/`): `useUnsavedChangesGuard(count, { onSave, saveLabel })` warns on tab close/reload (`beforeunload`), holds in-app link clicks anywhere on the page (window capture listener, before Next's `<Link>`), and now holds the **browser Back button** too: while the count is above zero the hook keeps one extra history entry for this same URL on the stack, so a Back press pops onto the real entry, is immediately re-pushed (the page stays put) and asks; leaving pops both (`history.go(-2)`), and the entry is dropped again (`history.back()`, suppressed) as soon as the count reaches zero. The dialog is Cancel / Discard / **Save** — the editor's Save button saves the draft ("Save draft and leave"), bulk edit's runs Sync updates ("Sync updates and leave"); a save that fails keeps the dialog open with the reason, and `onSave` is optional (no Save button without it). Bulk edit counts listings, ticked or not, with an edit no Sync has written. The single editor counts 1 while there are user edits newer than the last **explicit** save — the Save draft button, a schedule, the dialog's own Save — or Publish/Save to Etsy, or picked files still uploading to R2; the 2 s **autosave deliberately does not clear the flag** (it used to, which is why the warning almost never appeared: the autosave beat the user to the door). Edits are counted through `edit(handler)` wrappers on the user-facing handlers — every media handler and the single `onChange` that carries the whole listing form (Title … Variations … Settings) — so restores, copy prefill and Etsy seeding never count. An edit to an existing listing (reorder, alt text) now also creates a draft, so it isn't lost.
  - `next.config.ts` sets `experimental.proxyClientMaxBodySize: "101mb"`: `proxy.ts` runs on every route, and Next buffers — and silently truncates — proxied request bodies at 10 MB by default, which would have cut stored draft videos (and PSDs over 10 MB) short. The draft asset route also refuses a body shorter than its `Content-Length` (413).
  - **Bulk listing selection and editing.** `/listings` rows carry checkboxes with two select-all modes ("this page" and "everything matching these filters", the latter resolved to explicit ids via the unpaginated fetch). Selection survives pagination within one filter set and is dropped — for real, not just visually — when the state tab or section changes (`lib/listings/selection.ts`, a pure model). The header reads `Active — 3 selected` and, once anything is selected, offers Delete / Export / Copy / Edit. Delete confirms with a count and calls Etsy's `deleteListing`; Export builds a CSV in the browser; Copy creates one `ListingDraft` per listing (`sourceMode: "copy"`) and calls Etsy not at all; Edit opens `/listings/bulk` with the selection. Row hover actions are delete / copy / share / edit (the old unimplemented Merge stub is gone).
  - `/listings/bulk` edits each selected listing **individually**, and now covers what a single listing form covers. The sidebar is modelled on Vela's (`BULK_GROUPS` in `lib/etsy/bulk-edit.ts`): **AI Edits** (Title, Description, Tags — at the top), then **Media** (Photos, Videos), **Listings** (Title, Description, Tags, Materials, About, Production partner, Category, Section, Personalization), **Optional** (the 12 category attributes: Primary/Secondary color, Holiday, Occasion, Materials, Size, Sustainability, Sleeve length, Neckline, Clothing style, Graphic, Closure), **Inventory** (Variations, Price, Quantity, SKU) and **Shipping** (Processing profile, Shipping profile, Item weight, Item size, Return policy). One field shows at a time; Title/Description/Tags appear in two groups, so the selection is a `group:field` pair and the sidebar buttons carry `"<Field> — <Group>"` as their accessible name. Every group collapses, and the icon beside the header hides/shows the whole field list.
    - Top bar: the shop name above "Editing N listings" (from `/api/etsy/shop`) left; Cancel / Schedule / **Sync updates** right. Nothing reaches Etsy until Sync updates. Each row shows its thumbnail and full title above its own control; a master "Select all listings" checkbox beside the search ticks/unticks only the rows the search shows (indeterminate when some are). Rows are **virtualized** (`VirtualListingRows.tsx`, window scroll, measured variable heights, ~1200 px overscan) — a 60-listing test mounts only some rows.
    - Per-field bulk bar above the rows (`BulkApplyControl.tsx`), each with one Apply that writes **only to ticked rows** and stays disabled until the bar holds a usable value: Price is operation (Set to / Increase by / Decrease by) + amount + a $/% toggle (% not allowed for Set to); Quantity is operation + amount; SKU is position (Add before / Add after / Replace) + text (`lib/etsy/bulk-operations.ts`); Listings-group Title/Description keep the mode dropdown (Add before / Add after / Find and replace); Tags and Materials **add to** each row's list, never clearing it (`lib/etsy/bulk-text.ts`); dropdown fields get a "Choose <field>" dropdown, and an attribute whose properties have scales (e.g. Size) also a Scale dropdown — values and scales are matched across rows **by name**, since ids differ per category. Rows' attribute dropdowns pair with a Scale dropdown the same way.
    - **AI Edits** (Title/Description/Tags picked from the AI Edits group): a prompt preset, a model (Claude Opus 5 default, Sonnet 5, Haiku 4.5), free-text instructions and **Optimize**, which fills every ticked row (3 at a time); each row gets its own **Regenerate** with the same settings; both are disabled until a preset or prompt is set. `POST /api/ai/optimize` (`lib/ai/listing-ai.ts` validation/presets, `lib/ai/listing-optimize.ts` the call) uses `@anthropic-ai/sdk` `beta.messages.parse` with a zod structured-output schema, `fallbacks: "default"` on Opus 5, and holds answers to Etsy's limits (title 140, 13 tags × 20). Failures (no key → 503, rate limit, refusal) show on that row only; suggestions only fill the row until Sync updates. **Never called against the real API** — tests use a fake client.
    - **Variation-aware fields** (Variations, Price, Quantity, SKU, Processing profile): a listing with variations (and every listing on Variations) shows **the listing editor's own variation block** (`VariationBlock`, extracted from `VariationsSection.tsx`; `VariationRowBlock.tsx`) opened on the matching sub-tab; a listing without variations keeps its single control. Blocks are **collapsed by default** to a faded preview of the first combinations with a per-listing Show all / Show less, and the interactive table is only mounted when expanded. The inventory grid is converted to the variation form and back by `lib/etsy/variation-grid-form.ts` (free-text options get negative ids → `value_id: null`); a listing's patch carries the full replacement grid only when it differs from Etsy's. The Price/Quantity/SKU/Processing bars reach ticked variation grids too (every row, or the listing-wide value). A grid with validation errors is refused for that listing only (reported, block expanded). The old `VariationsCard.tsx` is deleted; `lib/etsy/variation-grid.ts` stays (still unit-tested).
    - Writes (`lib/etsy/bulk-apply.ts`, still the only writer, from `POST /api/etsy/listings/bulk/save`): `updateListing` for the listing fields (now also materials, who/when/supply, production partners, taxonomy, return policy, item weight/size with their units), the inventory record for price/quantity/SKU/processing profile **or** a full variation grid (never both — a grid already carries the per-combination values), one `updateListingProperty` per Optional attribute, and the personalization resource. One listing failing never stops the others.
    - Reads: `GET /api/etsy/listings/bulk` now asks Etsy's batch endpoint for `includes=Images,Videos,Personalization`, so Media and Personalization cost no extra calls. Attributes and variation grids have no batch form, so they are **one call per listing, fetched only when their group is opened**, via `GET /api/etsy/listings/bulk/attributes` and `.../bulk/inventory`. New supporting route `GET /api/etsy/return-policies` (needs no scope this app doesn't already request).
    - Every bulk route checks the ids against the caller's own cached listings first, so another user's (or another of this user's shops') listing is "not found" and is never sent to Etsy. **No migration** — nothing new is persisted beyond the existing `Listing` cache and `ListingDraft` rows.
    - **Sync updates writes one listing per request** (`POST /api/etsy/listings/bulk/save` with a single update; media first for that listing), so the run reports as it goes: the button turns into a spinner + "Syncing…", Cancel and Schedule are disabled for the duration, and a status line counts "Syncing N of M…". Each write is raced against a 30 s timeout (`writeWithTimeout`) that aborts that one request and fails that listing with "Timed out after 30 seconds." while the rest carry on; the run always ends in a `finally`, so the buttons can't stay stuck.
    - Save reports **per listing** ("Updated 1 of 2 listings. 1 failed." plus each failure by title, and on its row); failed listings keep their edits, and when anything failed the rows that landed are unticked so a second Sync retries only the failures.
    - **Variation photos** (`lib/etsy/variation-images.ts`): the inventory read also returns each variation listing's current photos (`GET .../variation-images`, one extra call per listing with variations), loaded into the block's Photos sub-tab, which offers only photos already on the listing (`etsy:` tiles). A patch carries `variationImages` — the listing's **full** set, `[]` to clear (Etsy's POST overwrites the whole set) — when it differs from Etsy's, or when the grid is replaced and photos are assigned. `applyBulkUpdate` sends it right after the inventory write: value ids are re-resolved by name (then by old id only if still present) from the PUT's response, or from a fresh `GET /inventory` when no grid was written; the listing's image ids are re-read and a photo not on it, a value that's gone, two photos for one value, or assignments on two properties refuse the whole set (never a silent drop). Images failing after the inventory landed returns `{ ok: false, partial: true }` — nothing rolled back, the cache mirrored, the row says "Partly saved.", and the photo selections stay so the next Sync sends only them. A listing whose photos Etsy didn't return shows a note instead of the tab. Sync now saves Media before the field patch, so photos are checked against the listing as media left it. In the single editor, `resolveVariationImageValueIds` no longer falls back to a pre-update id the saved inventory lacks, and a value it can't match is reported as a failed "Variation photos" step.
    - **Schedule queues the pending edits** (migration `20260918120000_schedule_bulk_edits`): `ScheduledListing` now carries `kind` ("publish" | "bulk_edit"), `bulkEdit` (the job) and `results` (per-listing outcomes). Schedule opens the same date/time picker the editor uses and `POST /api/schedule` with `{ kind: "bulk_edit", updates: [{ listingId, title, patch, media? }], setId }` — the screen's whole dirty state, serialised by `lib/scheduling/bulk-job.ts` (pure: parsing, `pendingBulkUpdates`, `summariseBulkResults`), with photos/videos a row adds uploaded first to `scheduled/{userId}/{setId}/media-{listingId}-image-NN` through the existing renders PUT (which now takes those slots and answers with the key). The route checks every listing against the caller's cached listings and every file against storage (`lib/scheduling/bulk-request.ts`). The runner branches on `kind`: `lib/scheduling/bulk-publisher.ts` applies each listing's media (the same plan/apply pair the media route uses, reading bytes from R2) then its patch through `applyBulkUpdates` — including the variation-photo step — and mirrors the listing cache, so one listing failing never stops the rest. Results are written on the row whatever happens; a job that didn't finish every listing counts as a failed attempt and its retry writes only the listings still outstanding. **With R2 unset the media step alone fails** ("file storage isn't set up…", result `partial`) and the field edits still apply; at schedule time a row that adds new files is refused with the same explanation while reorder/alt-text rows schedule fine. Scheduling clears the screen's edits, so no unsaved-changes warning fires afterwards, and `/schedule` shows the job as "Bulk edit · N listings" with each listing's outcome. Personalization and Production partner keep their richer row editors rather than a single dropdown.
    - `shouldAutoRenew` and `isTaxable` are still validated and saved by the API, but the specified sidebar has no slot for them, so the screen no longer offers them. Say so if the maintainer wants them back.
- **In progress:** nothing.
- **Known gaps (not yet asked for):** the unsaved-changes guard catches Back but not Forward, and its Back handling has only been verified in jsdom (a synthetic `popstate`) — not in a real browser; the existing-listing editor still has two buttons that write to Etsy — Save to Etsy (photos/videos only) and Sync to Etsy (everything); the multipart render/publish route with videos over 10 MB was also subject to the proxy truncation until the body-size change above — publishing a large video has never been tried against Etsy; **TODO (with the password reset flow): an emailed unlock link that clears `emailLocked` on the account's `auth_throttles` row (`accountKey(email)`); until then only deleting that row by hand unlocks it;** the IP limit trusts the forwarded-for headers, correct only behind a proxy that sets them; JWT sessions mean a password, email or 2FA change doesn't sign out other devices; no email verification on email change (no email-sending flow); a run interrupted mid-publish is only recovered after 30 minutes (`STALE_PUBLISHING_MS`); the runner refreshes Etsy tokens with the stored refresh token, which Etsy's docs don't say invalidates the browser session's copy — if a reconnect is ever needed after a run, that's the first thing to check. **The full listing sync has only been verified by build/lint/types and unit tests against an in-memory Prisma fake — never run against the (now fully migrated, per `prisma migrate status`) DB or a real shop.** First things to check there: the raw upsert's `text[]` casts for tags/materials and double-precision weights through the Neon adapter; that `getListingsByShop` really returns `skus` (documented for the owner's token — if not, the listings table's SKU column goes empty); how long 704 inventory calls take in practice (~3 min at 4/s). **The whole scheduling flow has only been verified by build/lint/types and unit tests — it has never run end to end against a migrated DB, R2 and Etsy together** (R2 alone is now verified — see Storage). The same is true of **bulk editing**: build/lint/types and unit tests only, never a real shop — including **scheduled bulk edits**, whose runner has never applied a job against Etsy. Their `kind`/`bulkEdit`/`results` columns **are now on the DB** (`20260918120000_schedule_bulk_edits` applied 2026-09-18; `prisma migrate status` reports the schema up to date) — until then every Schedule from the bulk editor failed on the insert, which is why the picker appeared to do nothing. Several things it deliberately refuses out loud rather than guess at, because Etsy's Open API v3 spec documents no semantics for them — emptying a listing's tag or material list, moving a listing to "no section", clearing a category attribute, clearing an item weight/size, and emptying a variation grid (removing a property's last option). Editing price/quantity/SKU **on a row** is still refused for a listing with variations, since those price and stock each combination separately; that listing's inventory is edited on its Variations card instead, which sends the whole grid. `description` gets no character counter because neither the spec nor the Listings tutorial documents a limit for it. **Bulk variation photos have never run against a real shop either** — Etsy doesn't document whether `variation_images: []` is accepted as "clear all", nor whether deleting and re-attaching a photo (what a Media save does) keeps its variation-photo links; photos added in bulk Media can't be picked for a variation until a Sync has uploaded them. **The photo save's re-rank call has run against a real shop only in the probe above** (not through the app's routes). Not yet tried live: clearing a photo's alt text with an empty `alt_text` (the final re-read reports it if Etsy keeps the old text), changing alt text through a re-rank (the probe re-sent the existing text), adds/removals through the app, and video order (delete + re-attach in slot order, which Etsy doesn't document as controlling display order). The connection has no `transactions_r` scope, so "no recent sales" for the probe listing was judged from its untouched 999 quantity, not from sales data. After a successful Save to Etsy in the editor, rendered/own tiles stay in the grid, so saving again uploads them again; Sync to Etsy resets the grid to Etsy's photos.
- **Pending manual steps for the maintainer:**
  - Add `TURNSTILE_SITE_KEY` and `TURNSTILE_SECRET_KEY` (Cloudflare dashboard → Turnstile → Add widget) before deploying — in production without them no once-locked account can sign in. Local dev uses Cloudflare's test keys. Documented in `.env.example`.
  - Add `ANTHROPIC_API_KEY` to `.env.local` for the bulk editor's AI Edits — until then Optimize/Regenerate report "AI Edits isn't set up" per row. Documented in `.env.example`.
  - Add `SCHEDULE_RUNNER_SECRET` (min 32 chars, `openssl rand -base64 32`) to `.env.local` — until then `POST /api/schedule/run` refuses every request with 503 and `npm run schedule:run` exits with that message.
  - Add `TWO_FACTOR_ENCRYPTION_KEY` (min 32 chars, `openssl rand -base64 32`) to `.env.local` and restart — until then enabling 2FA shows "not available" and the server logs the missing variable.
  - **Deleting listings needs the `listings_d` Etsy scope**, which is not in the default scope string: set `ETSY_SCOPES=listings_r listings_w listings_d shops_r` in `.env.local` **and reconnect the shop** (Etsy fixes an access token's scopes when it is issued, so an existing connection keeps the ones it was granted). Until then the listings page's Delete reports Etsy's 403 against each row and nothing is deleted; every other bulk action is unaffected. Documented in `.env.example`.
- **`.env.local` is maintained by the maintainer only — never edit it** (except when the maintainer explicitly asks; `ADMIN_USER_IDS` was set to the maintainer's id that way on 2026-09-18 — restart `next dev` to pick it up). Document any new variable in `.env.example` and tell the maintainer its name.
- **Storage:** Cloudflare R2 is configured (`R2_*` in `.env.local`, bucket `listhouse`). Verified 2026-09-18 through `lib/storage/r2.ts`: a test object was uploaded, read back byte-identical and deleted. No app flow (draft save, scheduling, bulk media) has used it yet. Do not touch storage config.
- `TODO.md`'s five tasks all appear shipped (commits 7b3b227, dd060aa, 8575b3c, 72de813, c6953bd); the file itself hasn't been updated.

**Architectural direction:** users will NOT upload PSD files. The product maintains a curated library of pre-calibrated mockup templates as flat JPEG/PNG. Users upload only their design PNG. The PSD pipeline stays in the repo because it's still used for the maintainer's own Etsy shops, but it is no longer part of the product path. Do not change the mockup compositor logic unless explicitly asked.
